# Campaign 5-4

- 2026-09-06: Inspected all six reports in `bugsarcturus.zip`. Twin Laser now uses two independently animated `SweepingBeamMarker.Noun` endpoints with the recovered cast-target offsets, hold, inward acceleration, and end-animation timing. Cast-owned tracking cleans up both markers without clearing a newer cast's effects. Missile flight now participates in shared freeze/gravity/destruction control and resolves at its actual impact position. Build/client verification remains outstanding; see `notes/combat/arcturus.md` for compatibility decisions.
- 2026-09-06 continuation: Saw Blade Shot now replaces Twin Laser outside laser range, the aerial sequence drops twelve missiles and resumes combat after landing, and the passive rolls for ranked scarabs every second. See `notes/combat/arcturus.md` for recovered contracts, implementation limits, and verification status. These are implemented server paths, not yet client-verified fixes.

- Current: All reused tiers have ranked combat and localized long-description authority, and RED-D-TOR has authoritative Carapace mitigation.
- Destructor encounter: base 5-4 now selects packaged `CitadelBoss.Noun` (Arcturus, the Cybernetic Colossus) at the authored final-arena anchor, preserves the imported boss profile and shared final-boss lifecycle, and uses the 480-frame `ctd_boss_tc_death` sequence with a rounded 17-second presentation boundary. Later `infinity_4` revisits retain their ranked Captain selections.
- Arcturus combat evidence: indexed Lua recovers Twin Laser as a target-facing three-part channel with paired beam and muzzle events terminating at a collisionless floor endpoint, 0.5-second pulses, 15/17.5/20 ranked range, and 3-6/4-8/6-12 damage. Production emits that complete presentation, sizes its channel from the authored 0.1-second-per-meter hold plus 0.5-second sweep, and cancels damage when range or obstruction invalidates the beam. `CitadelBossLaunch` is separately confirmed as a 30-second aerial action with twelve missile drops at 0.5-second intervals.
- 0.7.4 report evidence: mission index 20 reached the valid `infinity_4_Design` encounter boundary and admitted object 401 at the authored boss anchor `(1042.034,-265.747)`, but the running binary selected `CitadelSpecialTwo_Captain.Noun`; the base-slot catalog correction directly replaces that stale RED-D-TOR selection with `CitadelBoss.Noun` without changing the proven trigger or anchor.
- Evidence: `notes/campaign/5-4/descriptions.md` records all fifteen families, including Citadel Specific Two's native melee-taunt modifier.
- RED-D-TOR: base `CitadelSpecialTwo_Captain` now uses the recovered 80 HP, 100 power, 10/10/10 core attributes, 60 dodge/resist, 45 critical, 2.1 scale, and 1.05 footprint profile with explicit `Carapace.NPCAffix` identity.
- Carapace: each incoming damage packet is capped after ordinary reductions and vulnerabilities at `floor(6 * 1.037500023841858^difficulty)` until RED-D-TOR is destroyed; checkpoint restoration reconstructs the permanent affix. The `_2` and `_3` named bosses remain unaffected because their authored identities are different.
- Taunt boundary: native GUID `0x5ad04855` has no recoverable duration or forced-target definition in the indexed Lua corpus or canonical native decompile, so production retains the exact ranked hit and presentation without inventing unsupported taunt authority.
- Campaign coverage: `infinity_4` and its 436 authored markers apply to 5-4, 10-3, and 13-2 with explicit ranked actions for all 15 roster families.
- Verification: `mage build` completed successfully on 2026-08-13 at the campaign cleanup boundary. Tests were not created, modified, or run because repository policy prohibits them without an explicit request.
- Remaining: perform a human completion run.
