# Pickup pursuit reports, 0.7.30

- Equipment targets 342 and 350 entered client pursuit without a movement publication or timeout; target 350 was only 0.058 units outside server admission. Pickup pursuit now publishes the existing player approach contract with a stop point inside unchanged pickup reach and uses the shared eight-second pursuit timeout. A replacement pickup or actor/session change invalidates the old timeout. This addresses the stalled approach path without granting distant items; live verification remains outstanding.
- The contemporaneous projectile-disappearance report contains no shot/impact record in its captured minute; client scene events are clock traffic. Projectile collision and fog behavior were left unchanged pending a capture containing the shot. All three archives were inspected with native 7-Zip; extracted reports are under `bin/game/logs/pickup-0730`, and the projectile client trace is under `bin/server/darkspin/logs/traces/projectile-0730`.
