# Objective

- Current: All packaged definitions load; campaign zones use the five-row compatibility selection while tutorial retains its single obelisk challenge, every admitted co-op slot is activated, permanently departed slots are removed while reconnectable members retain progress, and authoritative damage, NPC healing, player/NPC/destructible death, hostile Root/Slow creation, distinct equipment/health obelisk events, full-clear, restart, and completion state feed packaged callbacks. Restart validation rejects missing member activation, active non-member slots, duplicate records, and invalid medal states before restored objective authority is admitted. Completion isolates callback failures, evaluates each active co-op slot, and remains available until every member has entered its own result transaction. The runtime can also invoke proven custom callbacks by stable objective instance ID; `CrystalFound` and its `SetObjectiveIntDataFromSPID` mutation now compile into ordinary objective updates.
- Remaining: Recover the retail pool draw policy and add the quest-crystal world drop/pickup producer that invokes `CrystalFound`. The exact packaged request is `nGameObject.DropObject(loot_questCrystal.Noun, defeatedNPCObjectID)` after each 0.135 kill-fraction boundary; its distinct world-object and pickup packet contract still must not be replaced with ordinary catalyst inventory. Broader modifier producers can be added when their semantic descriptors are retained by gameplay authority.
