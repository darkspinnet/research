# Campaign 1-3

- Current: All three occurrences have their ranked roster actions, authored movement, and long-description behavior, with first-visit Mizod, Catalyst Unlock, results, and Cash Out authority.
- Evidence: Animus starts each revived enemy's first action immediately and returns to ranked Ghostly Bolt/flee combat during its retained resurrection cooldown; its intentional flee-family pursuit is no longer rejected against the primary resurrection family. Lightning Juggernaut now uses footprint-consistent charge pursuit, publishes its authored +8 movement state to the client, retains its intentional secondary Smash chase, and follows the packaged `DeathDetonate` contract: `cast_explode`, 1.3-second hit and corpse deletion, seven-unit Energy AoE at the defeated position, electric impact, and 8–16/12–24/16–32 ranked damage. The imported 1-3 design set resolves four ordinary tunnel edges and its final threat-gated boss-security edge; ordinary caves no longer receive the oversized boss-teleporter projection. Draining Simian's `Sleeper.CharacterAnimation` noun reference now selects the compatible generic biped fall state instead of the unrelated Zelem fallback. Existing Necrotic Leech, Draining Simian, and Necrodactyl tier profiles retain their recovered locomotion and abilities.
- Remaining: Human-validate natural traversal and completion, then replace the documented population, Mizod publisher/add-phase, Catalyst destination, and objective-selection compatibility fallbacks if authoritative retail-server evidence becomes available.
# 2026-08-26 boss admission regression

- The 0.6.5 report reached `nocturna_4_design.Markerset` at the final arena while the server repeatedly observed the inert `nTutorial_CatalystUnlockClient.main` trigger and never admitted the packaged `lvl 4 boss arena` listener graph.
- Later-level boss admission now has a server-owned proximity publisher at the authored `SpawnPoint_DirectorBoss` anchor, while the named event transaction still guarantees only one accepted boss encounter per zone instance.
- The reported repeated death of object 205 is an authored Rezzer encounter: `Rezzer.Noun` revived the defeated `NocturnaBasicHealthDrain.Noun`, which was then defeated again nine seconds later. The server trace supports retaining that resurrection rather than suppressing it as duplicate damage.
- The reported item collection committed crystal object 81 and saved the safe-pickup checkpoint one second later; no failed item admission or uncommitted pickup appears in the captured exchange.
