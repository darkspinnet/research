# NPC facing implementation

Implementation completed 2026-09-04; targeted production compilation and `mage build` passed, real-client visual acceptance pending. Read [the evidence and native contracts](../combat/npc-facing.md) before editing. Generated evidence is in `bin/game/logs/mob-facing-2026-09-04`.

## Ordered action items

- [x] **P0 — Repair shared attack turns.** Replace `AttackStart`'s ineffective flags-`0x20` facing with the native captured-point turn (`0x42`, actor root goal, normalized facing, target position, target ID zero) before animation when the authored action permits turning. Do not append `0x95` to the turn. Cover `ChargeStart` and the direction-preserving intent in `ChargeCleanup` as well. Preserve heading on self-target/coincident-point casts instead of inventing the helper's fallback `+X` facing. Owners: `server/zone/npc/raknet103/action.go`, NPC action plans, and their callers.
- [x] **P0 — Stop erasing orientation at arrival.** Make ordinary `MovementStop` preserve the current pose instead of sending `ObjectTeleportMessage` with an all-zero quaternion. Audit first-action-in-range, pursuit arrival, cancellation, blink, growth, charge cleanup, and target forced movement. Use position-only correction plus a stop to preserve the existing client quaternion, including relocation paths; verify their native interpolation in the real client. Replacing zero with identity everywhere is insufficient because it still discards heading. Owners: NPC adapter, `server/gameplay/npc_pursuit.go`, and associated action callers.
- [x] **P1 — Encode one coherent spawn transform.** Remove the conflicting identity reflection from enemy creation when the Euler prefix owns orientation, or publish an equivalent explicit quaternion. Include the owned-summon `ObjectCreateMessage` branch. Preserve fixed actor placement rotation, propagate needed placement provenance through population/listener/horde/boss plans, and make generated-actor facing policy explicit. Initialize a valid feature-facing state before exposing a spawn. Do not copy camera or unrelated marker rotation; record any adopted procedural policy fallback in `notes/help.md`. Owners: `server/zone/npc`, population/horde/boss, `server/game/campaign_director.go`, and create-message adapters.
- [x] **P1 — Preserve authored facing policy independently of animation.** Carry turn-on-create policy, inherited defaults, explicit false overrides, and intentional pose locks in feature-owned action data. Audit all 34 current gameplay calls to `AttackStart` and the separate stationary/first-aggro/projectile paths. The evidence index includes 29 explicit-override chunks; 24 assign false. Preserve shielder setup/grenade/bash, Pouncing Stalker's jump-melee continuation, robot activation, cower, and other documented non-facing actions. Reproduce `FirstAggro_FaceTarget` even though it has no attack animation. Do this together with the P0 turn change so exceptions do not regress.
- [x] **P1 — Separate caster turning from projectile aiming.** Retain root/cast target inputs independently of muzzle position, lead, and spread. Give later shots an explicit facing operation when `trackBetweenShots` requires it even if activation animation is suppressed. Native object-facing uses `0x102` and a live target ID; ordinary cast-facing uses `0x42` and a captured point. Do not change projectile aiming or collision rules just to make the model turn. Owners: `npc_projectile_zelem.go`, projectile activation inputs, and simulation/adapters.
- [x] **P1 — Keep authority, observers, and restore consistent.** Commit semantic facing at accepted action/movement boundaries with the existing generation guards; distinguish pursuit direction, strafe target-facing, locked shield/charge pose, and teleports. Publish the same intended turn to source and observing players. Audit `ActionEventAttack` reconstruction so it neither substitutes a generic attack pose nor conflicts with simulation output. Restore current snapshot facing rather than replaying only spawn rotation. Owners: `npc/session.go`, `npc/pursuit.go`, NPC projection events, `gameplay/handler.go`, and checkpoint projection.
- [x] **P2 — Restore idle facing.** Implement the authored combat-idle turn threshold (30–330 degrees) and `0.1 + random()*0.5` wait at the appropriate idle leaf. Keep active attack/channel/charge locks, disabled actors, target changes, and action generations respected. Avoid replaying complete movement goals every tick. Owner: combat-idle/strafe scheduler and a named facing operation.
- [x] **P2 — Reassess boss sign inversions after the shared fixes.** The current `sca_boss_attack*` and five exact-animation sign exceptions cannot affect facing under flags `0x20`. Remove obsolete compensation when adopting the proper native contract; retain a model-specific adjustment only if a fresh capture proves a root-versus-graphics mismatch after correcting publication. Check Polaris, Orcus, Merak, Arcturus, and The Corruptor across affected animations. Do not patch packaged animation/model assets or add a Fang gameplay hook.

## Real-client acceptance

Verification completed: `go build ./server/gameplay ./server/zone/...` and `mage build` passed. `git diff --check` passed, and no test files were changed or run. Use the existing observational client/server path and keep protocol captures under `bin/server/darkspin/logs/traces`; extracted diagnostics belong under `bin/game/logs`. Do not create, modify, regenerate, or run tests without a new explicit user request.

| Scenario | Required observation |
| --- | --- |
| Fixed tutorial actor with nonzero authored rotation | The initial create pose preserves its authored direction, including before aggro/reveal. |
| Generated pack, horde, named boss, and owned summon | Each has a valid initial orientation chosen by the documented placement/introduction policy. |
| Ordinary melee starting already in range | The first accepted strike turns toward the captured target; no prior walk is required to orient it. |
| Pursuit followed by melee or special attack | Arrival does not install zero/identity orientation or undo the attack turn; root and attack presentation remain aligned. |
| First ranged shot and a ranked tracking volley | First-shot turning follows policy; later tracking turns work without replaying animation. Non-tracking shots preserve their authored pose. |
| Shielder, cower, robot activation, jump-melee continuation, self buff | Explicit non-facing and retained-pose cases preserve their intended heading. Shield directional rejection agrees with its rendered facing. |
| Stationary combat idle while the target moves around the NPC | An eligible idle actor turns at the authored threshold; an active locked attack does not inherit idle turns. |
| Blink, stun/sleep cancellation, growth, charge completion | Position changes and stops retain a valid intended orientation through the next action. |
| Two players, target swap, and restored/rejoined zone | Both observers see the same intended facing, and saved facing survives reconstruction. Retired action callbacks cannot reapply an old turn. |
| Named boss animations formerly in the inversion list | Compare actor root forward and rendered body separately in several approach directions; no blanket 180-degree compensation hides a packet defect. |

Capture noun, object ID, action generation, actor root position/orientation, intended facing mode, target point/ID, and emitted `0x8C/0x90/0x91/0x95/animation` order. Use existing diagnostics where possible. Distinguish smooth native turning from a stale heading; do not introduce a new turn-delay or combat-timing policy without evidence.

Implementation is recorded in the changelog. The scenario table remains an uncompleted real-client acceptance checklist; compilation is not visual verification.
