# Co-op instance

- Current: Shared zone membership, generation-safe members, unique per-slot hero blocks, per-member collision footprints, stable host identity, deterministic complete and restart-restored Blaze rosters, expected-member ready barriers, frozen participant-count combat scaling, checkpoint retention, immediate connected-peer delivery with queued fallback, semantic cross-peer hero projection, action-presentation fanout, synchronized pickup removal, and target-owned hostile damage, defense, death, reaction, and stat transitions work. A feature-owned in-memory party aggregate now owns create, online-only invite delivery, invite expiry, invited join, leave, leader promotion, destroy, native Messaging invite forwarding, authoritative party chat, login reconciliation, and Playgroups projection. Shared-lobby admission publishes case-insensitive peer identity, sends the local member event first, and then replays pre-existing members; an observational client diagnostic now separates an empty native room vector from unresolved SDK user pointers. Empty-target playgroup creation now recovers to a native invitation only for one unambiguous authenticated peer, without a behavior-changing Fang hook. Starting a game as party leader freezes every active member into a compact stable slot, rejects non-roster admission, projects one complete connecting-state game setup to every member, and encodes the authored indirect-join union with the reserving playgroup identity for each non-host. Independently matched four-player rosters instead use the authored matchmaking setup union for every entrant, carrying that entrant's matchmaking session, live Blaze player-session identity, and created-or-joined success result while projecting those same live session IDs in the roster and topology host fields, so they do not require a nonexistent party playgroup and every Map Room can complete its exact queued session; `NotifyMatchmakingFailed` remains reserved for rejection and cancellation. Mesh updates now record each player's readiness without publishing partial roster transitions; once the complete frozen roster is ready, one publication broadcasts every connected and join-complete transition and enters in-game. FinalizeGameCreation enters pre-game without impersonating non-host readiness. RakNet admission publishes each connected peer with `PlayerJoined` and withholds `PartyMergeComplete` until the frozen participant count is connected. A failed restored-member setup rolls back only that member, Start Fresh retires all live and retained gameplay ownership, and restart restoration reserves disconnected partners without carrying obsolete transport generations into the new process. Encounter completion retains the shared zone and its recovery checkpoint until every admitted or restored member has committed an independent result, then enters one shared vote epoch. Replacing a transport generation preserves that member's outcome phase, result receipt, and existing vote.
- Remaining: Re-run native two-client 1-1 entry and confirm Rawr receives create context in slot 0, Foo receives indirect-queue context in slot 1, pre-game precedes in-game, both clients open RakNet Hellos, merge occurs only at connected mask `0x3`, and both survive the first dungeon baseline; recover the exact build-103 create-success continuation for multi-peer target selection, verify leader-leave presentation, and complete beam presentation parity. The `20260820T105915.881969700Z` run showed Foo incorrectly receiving `CREATE_GAME_SETUP_CONTEXT`, while the 2026-08-27 11:14 run proved ordinary join context still makes the unsolicited reserved peer request removal with reason 6. The generated setup-context and state tables now select the dedicated indirect-queue route, and the next run records destroy reasons and mesh target values if another field remains incompatible.
- Evidence: The 2026-08-27 11:31 Foo/Rawr trace reached two RakNet peers and merged connected mask `0x3`, proving the indirect-join union. Foo then emitted an automatic Playgroups leave followed by create with `PlaygroupKey=1`; treating the intermediate leave as permanent split its visible roster. Active-game handoff now preserves membership and the recreate response reuses the existing party without replacing its host network. The 11:37 trace merged both peers but opened the chain countdown only for Foo, proving queue advancement remained connection-local; countdown and preparation now advance the complete game roster from the first accepted member command. The 11:44 trace proved the host emits finalization while the non-host emits the only mesh update, so host-only readiness is now recorded at finalization and remaining members are still independently gated. The 11:48 trace showed Rawr's preserved recreation still cleared its local roster; existing-party recreation now replays the complete member snapshot only to that requester. Both peers then reached status `4` progress `1`, but each saw only its own status; identity and loading statuses now project to every game peer.
- Evidence: The 2026-08-27 15:14 run formed Foo, Rawr, Three, and Four into one game, reached RakNet mask `0xf`, delivered the chain vote to all four, and later ran all four in the dungeon. Three alone skipped queue presence status `5`; Three was also the first mesh reporter, while the three later reporters published status `5`. Connected and join-complete publication is now held until the complete mesh-ready barrier closes so the first reporter cannot receive roster transitions during its matchmaking-finished callback.
- Evidence: The action handler previously returned `/dance`'s `SetAnimationState` and each attack's pose/presentation packets only to the commanding transport. Delayed attack producers likewise returned release, projectile, effect, and modifier presentation only to the actor. Immediate accepted-action presentation now queues to every other current member of the exact shared zone, and guarded delayed producers repeat that fanout only after their actor-facing send commits; the peer allowlist excludes action acknowledgements, cooldowns, and local-control updates. The 15:18 four-player trace confirmed every `/dance` start fanned out to three peers, but its later movement reset remained only in the actor's direct response because movement uses a semantic peer event. That event now carries the accepted movement's dance-stop boundary and emits the same animation reset to every peer. The 2026-08-27 12:05 trace also proved both baselines encoded slot 0 at `(-122.597,-154.484,10.088)` and slot 1 at `(-118.372,-155.755,10.088)`, but the earlier-started host never received object creation for the later member's objects `4..6`. The later member did receive host objects `1..3`, and movement only reconciled the missing remote hero afterward. Initial roster publication now directly queues the later member's complete object, resource, visibility, and deploy snapshot to peers already in the exact zone; peers not yet in the dungeon continue to receive it through their own baseline.
- Evidence: Peer presentation had remained buffered until the recipient's next RakNet poll, which can arrive after a short attack animation has already completed. Each admitted transport now retains its autonomous scheduler, so immediate and guarded delayed action presentation is sent as soon as it commits while the bounded queue remains a fallback for scheduler rejection. Equipment pickup roll/removal publication uses the same path, and successful crystal collection publishes only its world-object deletion to peers while keeping the acquired-slot result private to the collector.
