# PVP

- Current: Arena packet layouts, four-player/two-team Blaze criteria, matchmaking lifecycle, local PVP deck selection, six shipped PVP maps, the four authored staging/combat spawn groups, per-player result totals, and per-round hero health/mana percentages are recovered. Both direct party 1v1 and matchmaking 2v2 now randomly select one shipped Arena, stage teams at authored Team1/Team2 variants with squad swapping available, and begin a shared five-second opening transition only after every player's dungeon setup commits. The transition moves the teams to distinct Team3/Team4 combat variants and enables damage only after those authoritative teleports. New and normalized local profiles now project the client-proven first-PVP-deck unlock, lighting the native destination, and profiles without a PVP squad receive a persisted selectable starter squad copied from their complete active PVE trio. The shipped Map Room movie nevertheless hard-disables unranked 1v1 in both its selection and state-refresh paths before its otherwise valid solo play-button route; an explicitly approved, narrowly signed Fang runtime hook now changes both exact disabled literals while leaving the 2v2/custom gates, installed asset, and unranked request intact. A two-member party now contributes two roster slots through one leader-owned search, so that party plus two solo searches can atomically form one four-player Arena game with stable slots and one-based gameplay teams. Blaze continues to receive zero-based `TIDX` values into its zero-based `TIDS` list. All connected participants receive their complete initial gameplay-player roster before party merge and the Arena Lobby transition, each gameplay connection retries that state-gated transition once per second until it acknowledges lobby entry, and that acknowledged entry repeats the slot-ordered player baseline immediately before the lobby snapshot so an earlier asynchronous peer delivery cannot leave the native readiness gate closed. If build 103 does not accept subtype 5, the first unanswered branch retry starts the normal level-preparation handshake for every unacknowledged default PVP squad, followed by per-member retries until loading is acknowledged. Status-8 Arena loading projects member readiness and `GameStart` without entering the campaign-only director path that previously discarded both peers, then immediately publishes `GameDungeon`, creates the bound squad, assigns its controlled hero, and appends every opponent roster after the local setup. Client code proves the party route starts from a server-published nonzero `TeamId` playgroup attribute, its Arena roster logic interprets team IDs as one and two, and its party-wide unlock gate consumes peer subtype 7 capability state. Fang carries the logical port-42128 party RakNet channel through physical port 42127, and the server demultiplexes it into an independent relay session. Each client now receives its own private binding only for its local roster member and the masked relay endpoint for every peer, avoiding the identical-endpoint deadlock without exposing another player's address. Party-scoped UserSessions visibility and lookup preserve the packed avatar identity and typed presence, live user updates retain the full-data mask, and account search reports active peers as in-client rather than web-only.
- Evidence: [`pvp-flow.md`](../protocol/pvp-flow.md) connects `GameType=3`, `ExpectedPlayerCount=4`, `TeamSize=2`, `GameGameTypeRule`, `arenaSkillRanked_Rule`, playgroup status `1/2/3`, Arena opcodes `0xb3`-`0xb6`, the six fixed lobby/result records, the `PvPResults` ActionScript renderers, and authoritative content rows for `cryos_2_PVP`, `infinity_1_PVP`, `nocturna_2_PVP`, `scaldron_1_PVP`, `verdanth_3_PVP`, and `zelems_2_PVP`.
- Server gap: Lobby deck acceptance currently permits only the account's default PVP squad. Arena now creates, deploys, and cross-projects each player's bound hero roster, admits movement and voluntary squad swaps during preparation, performs the authored opening placement transition, and applies targeted basic, special-two, and random damage only to an opposing team after combat begins. Active abilities spend mana and reserve cooldown/release cadence, but use a conservative single-target damage adapter until their exact PvP area, projectile, teleport, and status policies are recovered. Developer `/victory` drives every participant through the native result-state handshake with the invoking team marked as winner, but normal elimination and multi-round authority are not yet connected. The native countdown presentation packet remains unidentified; the server owns the five-second timing even if build 103 does not display its packaged countdown SWF. Squad abilities, full passive policy, elimination, round/result, combat disconnect, and stat-commit authority remain incomplete.
- Handoff: Rebuild and verify a solo Crogenitor sees unranked 1v1, with both `solo_unranked_1v1_select_hook=1` and `solo_unranked_1v1_state_hook=1` in the Fang trace. Separately confirm the recipient-specific endpoint projection causes two profiles to establish Fang-enveloped logical port-42128 party transports over physical port 42127, produce `Party relay control` logs for subtype 7/8, and light the native party 1v1 route without learning each other's address. Recover or project the nonzero `TeamId` transition if capability exchange alone does not cause it. Then rerun the corrected four-player party-plus-solos path through Arena Lobby. Replace any disproven relay/team/lobby fallback and implement a dedicated Arena aggregate for deck readiness, authoritative best-of-three-compatible rounds, results, and atomic progression. Defer Juggernaut and Kill Race until Arena completes end to end.
- Remaining research: Capture the complete ranked/unranked `0x0d` TDF shapes, validate the implemented success-notification ordering and team fields, identify the native opening-countdown presentation event, recover the first `0xb5` sequence, result selectors/counts and per-round `field_04`, disconnect policy, and level-10 unlock ID `38` request.
