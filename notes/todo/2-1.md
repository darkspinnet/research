# Campaign 2-1

- Current: Complete roster and long-description authority across 2-1 and its `_2`/`_3` reuse tiers, including Dynosphere absorption, Fragbot Mech Energy Defense, Mending Tanglid ally focus and close-range retreat, Zelem Special Three's target-timed multi-contact Piercing Laser, pursuit of its selected Energy Buff ally across the authored 12-20-unit band, and named-captain boss admission only at the nearest authored final-arena anchor without depending on a transient client callback flag.
- Remaining: Human traversal, population-theme, boss completion, reward, and route-transition validation; retain the evidence-limited native Verdanth Basic Ranged fallback until an external trace recovers its numeric projectile contract.
- Evidence: The shared piercing path maintains one hit per target, damages every trajectory contact in travel order, and publishes tunnel-in and tunnel-out at each body boundary; Raytheoid now resolves at the selected target rather than delaying contact until a 50-unit endpoint. Its support pursuit tracks the originally selected living ally, while successful Energy Damage buffs hold the source support branch for the full 30-second modifier cycle.
- Evidence: The 0.6.9 boss report repeatedly published the empty `nTutorial_OverdriveUnlockClient.main` boundary at the final arena while the nearby authored boss marker remained dormant; the client-only boundary is now consumed and proximity admission selects the nearest matching server boss listener instead of the first listener in package order.
- Evidence: The 0.6.15 boss report placed the hero 8.6 units from `DirectorBoss`, but its one-shot client boundary had fired roughly 22 units away and was consumed before proximity admission; that permission now remains pending until the authored arena is reached. The paired Botanical Tunneler/Mending Tanglid trace exposed pursuit scheduling with raw center distance after attack planning had already adjusted for both actors' footprints, producing same-millisecond move/stop loops; cone and melee pursuits now retain the adjusted profile.
- Evidence: The 0.7.4 report crossed within 17.5 units of `SpawnPoint_DirectorBoss.Noun-166917082` but the boss session had already been consumed by the old `verdanth_1` no-match fallback, leaving three opening actors pursuing from the unrelated upper navigation layer. Named admission now requires the nearest content-proven boss listener to be inside the arena radius, preventing an earlier movement elsewhere on the map from arming the one-shot encounter. The earlier Botanical Tunneler and Swarming Herbipod traces separately showed fallback noun geometry with client/server aim drift of 6.726 and 2.872 units respectively; direct projectiles replace the client surface click with the authoritative fallback collision aim before trajectory scheduling.
- Evidence: The 0.6.17 Rooted capture showed Entangle's modifier repeatedly applied while an already-issued hero move continued without another movement command to reject, so root application now stops the authoritative motion immediately. The paired projectile captures identified ordinary and homing shots whose owner died before flight completed; source loss now emits deletion before either terminal resolution or a homing-flight reschedule instead of silently cancelling or retaining the run. Live Mending Tanglid observation distinguishes its repeated keep-away behavior from Magnetic Master's one-shot retreat, while its packaged low-health ally branch remains first priority.
- Evidence: The 0.7.0 loading reports reached `verdanth_1` preview, then rejected their first RakNet ping because `VerdanthSpecialOne_Captain.Noun` had no projected identity and consequently retired the peer before dungeon setup. Packaged content contains both the ordinary Botanical Tunneler class and a distinct zero-stat Contagion identity with Unstoppable affixes under the same noun family; the content projection now exposes that named identity through the director's Captain noun while retaining the canonical family's combat and physics data, and boss lookup can conservatively fall back to the rank-matched ordinary family identity instead of terminating the session.
- Evidence: The 0.7.2 report reached `(89.344,63.280,0.088)`, only 2.587 units from `SpawnPoint_DirectorBoss.Noun-166917082`, after observing the 2-1 overdrive boundary, but no boss was admitted. The proximity publisher incorrectly classified only standalone final bosses as named encounters, leaving 2-1 dependent on transient callback state; every cataloged named captain and final boss now receives the same dormant-session authored-anchor fallback.
- Evidence: The 0.7.5 report reached `(90.663,54.805,0.088)` after the Overdrive callback had already been accepted, proving its pending bit can disappear before final-arena movement. Named-boss proximity is now evaluated on every movement advance while the boss session remains dormant; the catalog and authored 18-unit arena anchor still reject ordinary map traversal.
- Evidence: The 0.7.6 report stopped at `(83.944,62.939,0.088)`, 4.237 units from `SpawnPoint_DirectorBoss.Noun-166917082`, without admitting Contagion. The authoritative content row proves that marker carries `nTutorial_OverdriveUnlock.main` with an empty event name, which the proximity publisher discarded before evaluating distance. Callback-only director listeners now receive the same marker-scoped internal identity in the session index and boss resolver, allowing that exact listener to prepare and commit without conflating unrelated empty callbacks or widening the 18-unit arena guard.
