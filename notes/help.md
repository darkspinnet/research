# Educated fallback decisions

- arcturus-landing-knockback: push surviving accepted-hit landing and missile targets two units through navigation-clipped forced movement, away from the impact; native GUID 0x0139de24 displacement and missile impulse force/mass conversion are unrecovered.

- hero-target-defense: Apply rating-based full-hit Dodge/Resist using mission rating conversion with a 75% cap, skip periodic avoidance rerolls, multiply independent percentage defenses after existing ability reductions, then subtract flat armor before shields; retail avoidance conversion, resist outcome, stacking, and flat-armor order remain unavailable in the client TakeDamage stub (notes/combat/damage-mitigation.md).

These are significant server-authority choices that could not be resolved from
the available client, content, or protocol evidence. Each line records the
local playable fallback and the evidence that would replace it.

- botanical-tunneler-cycle: Separate a 1.5-second intangible underground phase from burrow_attack1 emergence, with damage 1.3 seconds after emergence and eight-second poison ticking for 2–4 base damage every four seconds; packaged scripts prove the emergence timing and description proves poison, but the native burrow selector and linked poison tuning remain unresolved.

- captain-affix-replication: Publish authored affixes as permanent self-sourced, single-stack noun-owned modifier instances on spawn/rejoin using the existing elite duration and default rank/bind fields; the report confirms missing presentation, but retail instance allocation and rank/bind/start parameters remain uncaptured.

- generated-item-budget: Use the level/rarity ceiling and relative stat prices documented in notes/items/generation-budget.md for new campaign rolls; no agreed hero-independent DPS/survivability target or current-content playthrough establishes final balance weights.

- summon-sprite-healing: Keep each Healing Spirit within two units of its owner and heal three percent of the active hero's maximum health once per second; the recovered Lua proves its 30-second lifecycle and presentation but delegates follow and healing behavior to unavailable native pet authority.
- mutation-agent-spawn: From 2-1 onward, replace one ordinary escort at 5-10 percent of lieutenant-centered Spike population clusters with a Mutation Agent, scaling linearly by chain level and excluding all horde, named Captain, mini-boss, and Destructor encounter paths; packaged Lua proves its identity and combat behavior, but no recovered retail selector identifies its eligible levels, frequency, budget, or chosen anchor.
- campaign-unclassified-weapon: When a nonempty functional loadout contains no catalog-recognized weapon, treat its highest-level item as the weapon for base-damage scaling; captures prove both a complete Zrin loadout and Revenant's sole level-126 item can lose weapon classification, but do not retain the equipped rigblock identities needed to identify the malformed slots directly.
- raknet-peer-idle-timeout: Retain a peer for two minutes without inbound data, ACK, or NACK so transient loss has time to reconnect before address-scoped cleanup; a prolonged retail build-103 idle trace has not established the native threshold.
- playgroup-single-peer-invite: Recover an empty-target playgroup creation as an invitation only when exactly one other authenticated profile exists; build 103 queues the selected name but does not continue into Messaging under the current create response, and no wire field identifies a recipient when several peers are online.
- objective-debuff-descriptor: Classify committed hostile modifiers whose authored names contain Root or Entangle as `IsRoot`, and Slow or Stagnant as `IsSlow`; packaged objective Lua proves the descriptor bits and affected event contract, but current action profiles do not retain a separate authored debuff-descriptor field.
- zone-rejoin-grace: Retain disconnected campaign membership and semantic zone state for ten minutes, excluding absent heroes and companions from NPC targeting until rejoin; retail reconnect grace and empty-zone timer behavior have not been captured.
- hero-choice-entitlement: Award one bankable hero choice whenever a Crogenitor level first reaches an authored hero-eligibility milestone from level 4 onward, and repair missing balances from level and owned-hero count at profile start; the retail entitlement cadence has not been captured.
- campaign-enemy-xp: Use `2 * NonPlayerClass.challengeValue`, full party credit, per-kill round-half-up `Player XP Gained`, and no separate rank multiplier; tutorial bar estimates support the class multiplier, while multiplayer ownership and modifier rounding remain without a retail trace.
- campaign-1-1-director: Use radius 32 for Wanderers and 25 for Spikes because the 2026-07-21 live route missed the nearest opening candidates by about 31 and 24 units respectively under the former 6/12 fallback; consume each locus once; reset the five-hit Wanderer sequence on spawn or fifth miss; interpret 12%/6%/2% as total clumps of 2/3/4; seed Spike history Medium/Skip; use A/B/C Hard challenges 50/80/110 and half-rounded-up Medium challenges; retail radii, reset/clump semantics, initial history, and 1-1 budgets have not been recovered.
- campaign-1-1-traversal: Evaluate only the hero's reported current position, never the future movement goal; group each authored Spike marker set into one proximity candidate; on overlapping entries resolve only the nearest Wanderer and nearest Spike with authored ordinal tie-breaking; seed deterministic random from game ID and difficulty and initially treat performance as doing well; retail polling cadence, Spike grouping, tie order, random seed, and performance thresholds have not been recovered.
- campaign-1-1-pre-pad-population: Replace the overlapping negative-X Wanderer A/Spike A cloud with five exact authored traversal anchors and walkthrough-derived count bands `6/4/5/5/8`; include one supported captain draw in bands one, four, and five, use the package-exact Cannonator=`ZelemBasicHybrid`, Reparatron=`ZelemBasicRepair`, and Space Barracuda=`ZelemBasicRanged` label mappings, but select the remaining nouns from difficulty-filtered pools because retail roster selection is not recovered.
- campaign-1-1-opening-roster: Use the package-exact Cannonator=`ZelemBasicHybrid` and Reparatron=`ZelemBasicRepair` identities and split the final seven mixed actors as four Cannonators and three Reparatrons; the final mixed split and retail phase-evaluation cadence are not recovered. Cannonator's strict `Distance > 8` projectile/else-melee split and Reparatron's `ShouldRepair -> Repair`, otherwise `ArcWeldingMelee`, linkage/order and target policies are package-exact and no longer part of this fallback.
- campaign-1-1-secondary-boundary: Raise the first-clear ability boundary from 4 to 5 within radius 8 of the reported `(645.392,-2.394,15.106)` point and rely on the native ability-count presentation for Helix; the exact authored trigger volume and separate voice event identity are not recovered.
- campaign-1-1-reported-formations: Center the newly reported multi-actor first-clear groups on the captured player positions with small symmetric formation offsets; individual retail actor transforms are not present in the reports.
- campaign-1-1-composition: Draw Wanderers only from the difficulty-filtered minion pool; build each Spike from one captain plus minions with total size `ceil(challenge/20)` clamped to 2-6; use authored positions first and deterministic two-unit radial spacing only when the group outgrows them; the retail six-noun bucket, agent/lieutenant probabilities, exact challenge table, and intra-group placement policy have not been recovered.
- campaign-1-1-horde-activation: Run marker set 1827 before 1828, scope reused named events to their owning marker set, accept one alive participant entrant for the party, activate all local gates/blockers during combat, and admit the final arena after horde 2 at the radius-60 presentation boundary; retail route edges, listener scope/order, multiplayer admission, final boss publisher, and gate/blocker synchronization have not been recovered.
- campaign-1-1-final-arena-recovery: Admit the final encounter after Horde 1 when the player reaches the exact final-arena support trigger even if the route missed Horde 2's radius-five trigger; the first-clear client can reach that boundary without intersecting Horde 2, and the retired server's recovery policy is unavailable.
- campaign-1-1-horde-budget: Use normalized actor-slot budgets instead of claiming the unrecovered native float budget: horde 1 has one 3-slot wave, horde 2 has 2-slot then 3-slot waves with a 1.5-second gap, and the final arena has four initial add slots followed by the leader and two add slots; retail noun costs, caller budgets, wave overrides, counts, scaling, delays, random selection, and concurrency limits have not been recovered.
- campaign-1-1-final-arena: Use aura rank one/radius 12, Illust's exact inherited class profile and authored captain metadata, an initial four-agent add-only phase followed by Illust plus two agent-pool adds, leader defeat plus exhausted add waves and empty membership, and one `mbBossComplete` commit; the phase order follows the indexed walkthrough, while retail affix replay, exact counts/delay, clear predicate, director-field ordering, and success/failure race policy remain unresolved. `AuraUtils` itself is recovered: one child instance per admitted target, delete on exit, and delete all on cleanup, with no duplicate-enter refresh or stack.
- campaign-objective-selection: Fill the five client rows deterministically with `FinishLevelQuickly`, `DoDamageOften`, `TouchAllObelisks` when the level has loot obelisks, `DefeatAllMonsters`, and `HugeDamage`; the authored Default and Daily pools prove eligibility but not the retail draw count, weighting, seed, duplicate policy, or Hidden Bonus selection.
- campaign-1-1-hero-basics: Reject a hero's campaign basic ability when its authored definition cannot be compiled instead of substituting a generic attack; current verified content compiles and executes all 100 playable hero basics through six shared shapes. Orion's Lightspeed Tempest Basic alone uses the documented scoped substitution for the absent generic chain-counter aggregator; do not extend that alias to Citadel or replace future compilation failures with a generic attack.
- campaign-pickups: Publish create/component/lob reliably in receiver dependency order; reserve and durably commit rolled equipment before deleting it, retaining the pickup on capacity/save failure; restore 7.5% of each affected hero's maximum health or power so the proven base-stat tutorial amount remains 15 while higher-level heroes scale, consume full orbs, project lob destinations onto the activating hero's reachable BFX region, use a 2.5-unit contact radius, and tolerate one unit of replicated-pose drift while retaining the authored two-unit interaction range so marginal requests do not enter the crash-prone client pursuit path; original sender order, sampled destination, equipment-full behavior, persistence transaction, exact pose tolerance, and retail orb restore percentage/ownership policy have not been recovered.
- campaign-crystal-chance: Apply unit difficulty/chance scaling so a challenge-500 loot obelisk makes the recovered 75-in-100 crystal attempt, then use the exact difficulty floor and weighted definition selection; the native difficulty-indexed chance multiplier and retail random-stream ownership have not been recovered.
- campaign-equipment-generation: Use the recovered difficulty rarity bands and uniform eligible rigblock/affix vectors with independent deterministic streams; retail source-specific rarity overrides, pickup feedback, and random-stream ownership have not been recovered.
- campaign-cashout-item-context: Generate each participant's Cash Out items for the hero deployed when that participant's Beam Out is accepted; the client result record carries no creature identity and the retired server's Cash Out class/science selection policy is unavailable.
- campaign-equipment-roll-disconnect: Freeze multiplayer equipment rolls across connected nonterminal zone participants when the pickup interaction is accepted and durably grant a disconnected winner without replaying presentation; build 103 proves the in-simulator roll but not disconnect-time simulator membership or deferred winner feedback.
- campaign-enemy-equipment: At each accepted ordinary enemy live-to-zero transition, make exactly one equipment attempt with source amount `10`, yielding `10 * 0.45 * 0.01 = 4.5%` before known modifiers; use the defeated actor position and killing player's deployed hero as generation context, then share the obelisk equipment pickup transaction. The omitted retail selector enables more categories, but its ordinary-death source amounts, category chances, DNA/orb policy, and random-stream ordering have not been recovered.
- campaign-enemy-orb: At each accepted ordinary enemy live-to-zero transition, independently apply source budget `25`, yielding one 25% orb attempt through the recovered hundreds-plus-remainder selector; use live squad resource weighting and the shared campaign-drop random stream, and while an available hero is dead convert an accepted health selection to the exact resurrection capsule unless one is already present. Build 103 proves the health-side override plus session count and 30-second operands, but the meanings of those operands, ordinary-death source budget, difficulty scale, category ordering, and exact stream ownership have not been recovered.
- campaign-enemy-crystal: At each accepted ordinary enemy live-to-zero transition, independently use source amount `26`, whose recovered `source * 0.15` threshold admits integer draws `0..3` for an exact 4% attempt, then use the shared weighted mission-crystal lifecycle. The 4% rate comes from the campaign-footage reference sheet, not a recovered retail ordinary-death source operand; stronger-enemy scaling, category ordering, and exact stream ownership remain unresolved.
- campaign-enemy-dna: Use the user-directed 50% ordinary-enemy frequency in place of the native 25% threshold while retaining the recovered difficulty-1 gate and level-scaled `max(1,trunc(0.4*S*G*U*(DNADropped+1)))` amount. Until the ordinary-death source operand is recovered, use `S=11` for low-band 1-1 enemies; this centers difficulty-1 baseline drops near the walkthrough-visible `DNA +6`. Retain reliable `ObjectCreate -> mDNAAmount -> lob` and commit-before-field-12/delete ordering, do not expire DNA on the disproven 30-second fallback, and temporarily retain the 2.5-unit walk-over contact until the authored initial trigger/homing runtime is implemented. Multiplayer allocation, persistence timing, event/delete sender order, and random-stream ownership remain server authority.
- campaign-1-1-walkthrough: Use the recorded visible route as the placeholder behavior envelope—leave authored Wanderer and Spike loci dormant until traversal, then use repeated traversal-gated mixed packs with ordinary drops, a short delay before an Illust-labelled boss horde with adds, and collect-or-continue results—without treating edited timestamps, visible counts, mutation rolls, reward levels, or the recording's difficulty/version as exact server policy. Retail proximity radii and population selection remain unrecovered.
- campaign-1-1-targeting: Create ordinary Wanderer and Spike population as visible, targetable, and out of combat at their traversal activation boundary, then assign the deployed hero exactly once within radius 12 plus both footprints and publish the nonzero-target in-combat `0x99`; horde and boss admissions remain immediately targeted. The local radius follows the walkthrough presentation, while retail perception distance, threat weight, team, attacker-count ownership, and first-action ordering remain unresolved.
- campaign-idle-target-position: Reconcile an idle, published, pre-aggro enemy to the client's reported target position only when the matching cursor is within six units and the target is inside the acting hero's authoritative range; for a targetless held melee swing, allow the same correction only when exactly one idle enemy remains within 20 units of that cursor. The reports prove native client simulation can move an enemy far from its retained server spawn before first aggro, but exact retail pose ownership and correction bounds remain unavailable.
- campaign-1-1-death-handoff: On the first accepted live-to-zero transition, immediately prune the defeated actor from live threat and director membership, call `DropStuffForObject(defeatedActor, killingControlledAgent)` once with the omitted/native `-1` selector, keep resulting pickups shared, and only then evaluate one scoped horde completion or internal route/security clear; retail threat-prune timing, ordinary selector/context, director removal ordering, and any named route-clear publisher have not been recovered.
- campaign-1-1-enemy-actions: Use rank one at difficulty 1-24, evaluate phase gambits in authored order every 100 ms, pursue until native-equivalent admission succeeds, select Haster's nearest living ally with object-ID tie-breaking and self fallback, use the primary invisible/beam-in Snipe path, skip Zelem's durationless first-aggro wait, and place its blink at authored radius 8 on a deterministic actor-ID angle without navmesh sampling; retail rank mapping, gambit scheduler, retry cadence, ally selector, Snipe alternate selection, first-aggro duration, reachable-position sampling, and modifier wire lifetime have not been recovered.
- campaign-1-1-drone-punch: Use the Drone class Mind value as the primary attribute for its Technology Punch damage projection and publish no hit effect; the packaged ability proves physical/technology damage, range, clocks, and operands but does not identify a separate NPC primary-stat selector or hit-effect resource.
- campaign-1-1-horde-gate: Create the exact three gates and three blockers at scoped horde admission, delete those IDs at completion, and repel contact within two units to the last accepted position; chunk 199 proves the red beam-in/teleport/yield/beam-out sequence and adjacent door assets prove CloseDoor/OpenDoor event ownership, while retail instance allocation, synchronization packets, contact footprint, and the absent callback's destination-property schema remain unavailable.
- campaign-1-1-results: After accepted Beam Out preserve one immutable result; Continue atomically advances completion before replayably loading `zelems_3` through `B0`, while Cash Out idempotently commits progression and one deterministic level-11 rarity-weighted item. Commit the zone's content-authored modified kill-XP ledger at successful Beam Out, while keeping new DNA, medals, chain bonus, bonus consumption, and creature entitlements at zero; build 103 proves preview and XP-total presentation, but upstream reward selection, chain bonus, `0xB9` order, disconnect/load-failure policy, and local `0xAA` handling remain unresolved.
- passive-hud: Create each selected campaign hero's permanent icon-bearing passive modifier after its object exists and retain it while that hero is in reserve; squad swaps only change the controlled object and therefore HUD visibility. Retail may instead recreate passives per deployment, but no sender capture proves that policy. Use duration zero, overdrive zero, stack one, source self, and the recovered zero action-block flag.
- campaign-1-1-first-security: Use the recovered radius-2 trigger plus the entrant footprint and radius-20 threat query plus each enemy footprint, then send the player to the next authored route spawn at `(555.5919,-31.4069,-0.4774)` once; only the source-to-destination edge and server movement-sampling cadence remain fallbacks.
- campaign-1-1-security-route: Pair the six ordinary authored security markers reciprocally across the three section boundaries, entering section three at `(-621.0203,650.6170,0.6042)` and leaving it through `(-543.5760,546.1642,0.1581)`, then route the final red boss-security source at `(184.2345,558.9578,10.2027)` to the strongest content-correlated arena destination at `(928.1883,668.9045,0.0880)`, using the recovered shared contact/threat predicates; the authored coordinates and 2026-08-21 playtest direction are exact, but the seven retail target edges remain unavailable.
- campaign-swap-cooldown: Use a five-second server-owned prototype swap lock and write zero to character reflection field `4`; build 103 proves that a nonzero portrait wipe requires an absolute deadline in the client's private gameplay-clock domain, but no client-to-server application or RakNet field publishes its additive origin, so the retail duration and per-hero policy remain unverified.
- campaign-1-1-pursuit-tick: Advance enemy pursuit on a `100ms` server tick, replan a BFX corridor to the live hero every tick, advance through portal midpoints at the recovered noun speed, and publish each retained position through `0x95`; retail server cadence, path smoothing, avoidance, and dirty-flush policy remain unavailable.
- campaign-npc-spawn-speed: Publish each action profile's movement speed as both NonCombatSpeed (11) and CombatSpeed (12), with neutral MovementSpeedBuff (48), so the client can execute server movement goals; exact noncombat speed remains unavailable for nouns outside the recovered build-103 speed catalog.
- campaign-player-pursuit-timeout: Cancel a retained player pursuit with its matching type-2 response after eight seconds so a failed client continuation cannot permanently retain combat input; build 103 proves the cancellation contract but retail server timeout policy is unavailable.
- campaign-difficulty-scaling: Apply the recovered difficulty row live with exponent zero in the current single-player campaign runtime because build 103 initializes simulator `+12` to `0x00040000` and therefore its consumed low word to zero; the client proves `base * row[difficulty-1] * pow(1.20 or 1.05, exponent)` but does not prove who later owns that low word or the adjacent captain/elite inputs, so replace this fallback when retail authority is recovered.
- profile-remote: Resolve remote Account Profile among active or durable offline accounts by exact Blaze ID or unique case-insensitive display name and return a redacted public projection; fail closed for missing or ambiguous targets, and keep remote Hero Profile disabled because no proven owner key accompanies its account-local creature ID.
- abilities: Toss projectile creation uses the generic projectile envelope and yaw-relative local offsets (X right, Y forward) because build 103 proves the lob update but not the retired retail server's create fields or offset-axis convention; replace this fallback if a retail capture disproves it.
- abilities-fire-tempest-owner: Poll the living Fire Tempest Active elemental's strict 50-unit owner boundary every 250 milliseconds and reuse the activation-facing half-circle for replacement placement; packaged Lua proves the boundary and immediate replacement but not the owner-loop yield cadence, while the server does not yet retain a newer hero facing outside action requests.
- abilities-lightspeed-chain: Compile Orion's four Lightspeed Tempest basics by substituting the shipped Orion-specific stack counter only for Orion's missing `modifier_chain_ability_counter.lua` require, then load its shipped generic chain template and haste modifier normally. The missing aggregator body is not recoverable, so this deliberately assumes its Orion-side effect was loading that concrete counter; the unrelated Citadel require remains unresolved and receives no alias.
- abilities-zetawatt-line: Treat Zetawatt Beam's `GetObjectsAlongLine` as a 35-unit segment intersecting each enemy's authored footprint, with a 0.5-unit fallback only when footprint data is absent; build 103 proves the line endpoints but not the retired server's spatial-query thickness.
- abilities-shadow-ravager-angle: Classify the rear hemisphere as behind and grant no bonus for unknown facing; modifier chunk 864 proves the 25-percent rear and overdrive-only 30-percent rear-or-side attributes, but their angular cutoffs lived in the unavailable retail damage engine.
- campaign-player-footprint: Use a `0.8` compatibility footprint for known playable creatures whose exact noun physics were not imported; this prevents valid targeted actions from becoming response-less client locks, but exact per-hero footprint radii remain unavailable.
- abilities-enrage-refresh: After Enrage's authored 10-second cooldown, replace its active same-GUID Unique modifier at the hit deadline and restart the 30-second duration without stacking the damage bonus; build 103 proves Unique admission and the two durations but not whether the retired server refreshed or rejected the same-GUID request.
- abilities-ride-presentation: Let the accepted Ride the Lightning response drive the local client's authored blur and omit duplicate server `cast_blinkstrike` animation overrides; the 2026-07-22 playtest proves the overrides add an incorrect red presentation, but no retail multiplayer sender capture establishes what remote observers should receive.
- profile-feed: Use an append-only server `user_event` ledger plus a local message type for arbitrary milestone text, because the recovered retail feed renderer only defines friend-add, hero-purchase, and upgrade-unlock and cannot represent level-up or hero-availability events.
- profile-friend-feed: Treat AssociationLists type `1` as the durable friend relation, retain events under their owning user, and join only public Crogenitor milestones into the requesting owner's feed at read time; the packaged page proves its account-ID split into My Log/Friend Log, but no retained retail relationship/feed capture proves list-type numbering or which additional event classes friends could see.
- abilities-arc-weld: Implement Goliath's Arc Weld with the recovered `ChainHitTargets` contract: hit the admitted primary, then choose the nearest unvisited hostile within rank-one radius 7 for up to four 100 ms jumps, add 25% damage per jump, and saturate later links on beam effect 4. The client helper and reflected fields prove this policy; only retired-server batching and sender timing remain unavailable.
- campaign-invincitron-drone-policy: Create one untargetable `NomadDrone.Noun` with server-owned circular motion (radius 2, height 2, six-second revolution, 100 ms updates), use its live position for the baseline range-15 SentryDroneLaser at 0.7 seconds against Invincitron's target, and delete it with its owner; packaged content proves orbiting behavior but not the trajectory, requesting AI selector, or native rage timing transform.
- campaign-equipment-interactable: Give equipment containers a one-use `PickUpLoot` interactable update between object creation and loot data; build 103 proves this is the missing type-11 command gate, but the receiver requires each rarity noun to have constructed `cInteractableData`, which still needs live verification. Do not apply this fallback to catalysts: their crystal noun class and nonzero field `+148` prove the separate type-9 construction predicate.
- campaign-floor-introduction: Pre-bake ordinary groups from the difficulty-filtered minion and captain/special pools, project their anchors onto stable BFX connected components, introduce every non-ambush group when the first player enters its component, and publish a triggered Spike-centered ambush atomically; build 103 proves BFX connectivity and Spike/Wanderer marker identities but not the retired server's floor-entry timing, exact six-noun bucket, or ambush policy.
- abilities-plasma-wreath-reserve: End Blitz's active Plasma Wreath when Blitz
  leaves the field so its server-owned proximity scan cannot attack from a
  hidden reserve. Build 103 proves death cleanup but does not expose whether
  the retired server preserved unused lightning charges across squad swaps.
- abilities-plasma-sentinel-immunity: Admit Plasma Sentinel Shock and Burn
  against ordinary 1-1 enemies and reject them while Invincitron's shield is
  active; the modifiers prove `ImmuneToStunned` and `ImmuneToBurning` checks,
  but current campaign enemy profiles do not import those two attributes.
- campaign-level-entry: Place 1-1 players in a three-unit formation around its existing `SpawnPoint_Affix.Noun` fallback and retain authored camera-anchor positions for later levels; the original retail player-position and orientation policy is absent from build 103 and packaged content.
- campaign-npc-profile: Resolve the 68 zero-stat captain ClassAttributes rows from their complete same-family, difficulty-suffix-aligned parent combat profiles while preserving captain-authored metadata; retain rank-shaped scale and footprint fallbacks where captain noun physics remain unavailable.
- campaign-final-cashout: Treat 18-4 as terminal and route either client vote choice to Cash Out, retaining 18-4 itself only as the required results preview record; build 103 proves the 72-entry chain but the retired server's terminal vote payload and epilogue ordering remain unavailable.
- campaign-reinfection-voice: Omit the unresolved 2-3, 3-3, 4-3, 5-1, and 5-3 reinfection voice hashes instead of replaying Zelem's cue across the campaign; chain content proves those voice-over slots but their exact event identities remain unavailable.
- labs-player: Keep initial deck score at zero instead of sending squad size; exhaustive build-103 inspection finds only a field copy and the tutorial's bounded 0-to-2 increment, while the preserved server source comments out a nonexistent `Squad::GetScore` call, so any retail aggregation policy is an external source boundary.
- first-loot-tooltip: Preserve build 103's native one-shot editor tooltip; no server input to its simulator-local latch is known, and mutating that latch from Fang is outside the allowed client boundary.
- squad-cooldown-clock: Enforce the 30-second switch lock on the server and publish an expired field-4 deadline so a guessed clock cannot extend the client lock; build 103 requires a private local gameplay-clock origin that no recovered C2S packet exposes, so the native portrait wipe remains unavailable.
- vendor-transactions: Apply each semicolon-delimited purchase, sell, buyback, and flair request as one all-or-nothing persistence transaction so a rejected entry cannot desynchronize DNA and inventory; build 103 proves ordered client-side application but no retained retail response proves partial-failure commit behavior.
- vendor-flair-charge: Charge the full generated content price and accept exact DNA equality for flair conversion; build 103 proves the displayed full price and local flair mutation but performs no local funds check or debit, and no retired response preserves the authoritative charge policy.
- loot-frequency: ordinary 1-1 enemies use source amount 10 for a 4.5% single-player equipment chance while challenge-500 obelisks remain guaranteed; retail ordinary-enemy source amounts are not recovered, and the previous 22.5% rate visibly overproduced gear.
- campaign-director-pools: Project later chain levels' four director configuration ordinals as minion, special, agent, and captain while preserving unknown raw content labels; build 103 proves the same four-position structure across the catalog, but only 1-1 pool semantics are independently named.
- campaign-enemy-physics: Use neutral scale 1 and footprint 0.5 when a campaign director noun has exact same-instance ClassAttributes but no imported noun-physics row; the complete authored noun-physics catalog is not yet imported.
- campaign-interactable-variant: Select one authored weighted interactable marker-set variant deterministically from the match ID; the retired server's random source and multiplayer selection ownership are unavailable.
- campaign-preview-roster: Preserve the observed six-entry 1-1 roster, then fill later mission previews from the first six distinct difficulty-eligible director entries in authored ordinal order; build 103 and content prove the candidates and wire fields, but not the retired server's six-entry selection policy.
- campaign-later-hordes: Spawn one agent per authored `HordeSpawner_Register` anchor and complete later-level hordes after that single wave while retaining 1-1's recovered special cases; content proves trigger volumes, scoped anchors, and eligible agent pools, but the retired server's later-level budget and wave policy are unavailable.
- campaign-later-boss-plan: For named-event starts, select the exact difficulty-band captain noun at `DirectorTrigger_SpawnBoss` and one agent per scoped `HordeSpawner_Register`, except that independent Polaris evidence selects the matching `ZelemBoss` tier for `zelems_4`; content proves the listener graph and eligible pools, but the public boss association, add phases, and natural event publisher are unavailable.
- campaign-later-boss-publisher: Start a later mission's scoped boss named event when its highest-ordinal authored player-entry horde reaches terminal completion, retaining the diagnostic captain/add policy; content proves both encounter graphs but not the retired server's exact causal publisher or optional-route policy.
- campaign-polaris-phase-order: Cycle ZelemBossPush, ZelemMarkSeeker, and SummonGravityOrb after the initial Push, hand distinct abilities off at cast recovery, skip a new Gravity Orb cast while the prior field remains active, and check the recovered health-loss Blink before each transition; snapshot play confirms that waiting each distinct ability's full reuse cooldown leaves Polaris inert, but the retired server's exact phase polling and tie-breaking order is not recovered.
- campaign-polaris-orb-placement: Drive the recovered four-attempt cone sampler from the campaign's deterministic random stream: sample yaw uniformly across the authored arc and distance uniformly across 6-12 units, project with the navigation spawn query, accept only a projection retaining both bounds, and preserve 1.5-unit orb separation; the helper algorithm is exact, while retail random-stream ownership remains unavailable.
- tutorial-completion-transition: Publish the positive TutorialGameMsgs completion snapshot with the Beam Out presentation, then publish QuickGame/0 after the recovered 650-millisecond beam boundary; build 103 proves both client consumers and the new report proves the snapshot omission leaves first-run state stale, but the retired server's exact sender timing remains unavailable.
- campaign-party-vote: Resolve Cash Out immediately when any current member selects it, require every current member for Continue, and re-evaluate unanimous Continue when a pending member leaves; build 103 proves the requests and party/cashout response but not retail majority, tie, timeout, or disconnect policy.
- campaign-later-objective: Select the packaged `TouchAllObelisks` objective whenever a level's chosen authored variant contains loot obelisks, using that exact selected count; the callback and placed objects prove the objective is executable, but retail per-level objective pool selection is unavailable.
- campaign-objective-results: Send final selected objective records after accepted Beam Out and before chain voting, with all sixteen unknown per-player `0xb9` result bytes zero; build 103 proves the packet structure and final-snapshot role, but not those byte meanings or retail emission order.
- campaign-1-3-catalyst: Start the exact Catalyst Unlock program with Mizod admission and lob its one crystal from the boss anchor to a reachable player-adjacent point; packaged Lua proves the unlock/drop ordering and timing, but the retired publisher, source role, recipient placement, and destination sampling are unavailable.
- campaign-draining-simian-movement: Use Draining Simian's exact 5.5-unit combat speed for pursuit and permit a new channel after the prior eight-tick channel ends; packaged class and Lua data prove locomotion and the complete channel body, but no additional cooldown operand is exposed.
- campaign-draining-simian-death: Use `gen_death_melee_quad` only when Draining Simian's imported rig lacks an ordinary death state; its noun proves the Sleeper rig but an absent state still needs a compatible fallback.
- campaign-unmapped-death: Use client-present `gen_death_melee` only when neither the noun's imported rig nor a specialized death override supplies a state, instead of forcing the tutorial spider death; compatibility for rigs with no recoverable ordinary death entry remains unproven.
- campaign-necrodactyl-lob: Use the shared local X-right/Y-forward launch-offset convention and Necrodactyl's exact 6-unit combat speed for its parabolic stacking-poison lob; packaged class and Lua data prove locomotion plus the lob and poison operands, but the noun AI phase and offset-axis convention are unavailable.
- campaign-animus-resurrection: Poll on the authored 15-second cooldown and resurrect the nearest same-encounter corpse within 16 units so completed encounters cannot be reopened; packaged Lua proves the ability body but the retired noun AI's target scoring and phase selection are unavailable.
- campaign-lightning-juggernaut-charge: Use Lightning Juggernaut's exact 3-unit combat speed plus the authored +8 charge modifier, then select the separately authored Smash while in melee range; packaged class and charge/template data prove locomotion and no collision damage, but the retired Boomer AI's phase-selection policy is unavailable.
- campaign-pyro-phase: Repeat CitadelMinionMelee_Flamethrower after the exact unburrow phase with Pyro's 6.5-unit combat speed; chunk 801 supplies cry_minn_el_1_attack1 and its 0.43/1.2-second hit/release timing, but the retired phase selector is unavailable.
- campaign-vampiric-leaper-jump: Project the authored speed-24 Hopper attack through navigation-aware NPC pursuit, use the midpoint 1.125-second post-jump wait, and make a too-close hop stationary and harmless; packaged bytecode proves the jump selection, animation, damage, and radius semantics, but the ObjectJump message's four physics operands, native random destination, arc height interpolation, and exact random wait draw remain unavailable.
- campaign-pterodyne-movement: Use Pterodyne's exact 6-unit combat speed, select Stealth Attack whenever the target is outside Stealther Melee reach, choose a navigation-safe destination in the authored 5-10-unit band, and chain Fear Nova only after a surviving hit; packaged class and ability data prove locomotion, all three ability contracts, and the follow-up edge, but the retired noun phase's polling cadence, opener choice, and reachable-destination scoring are unavailable.
- campaign-carrion-shambler-movement: Use Carrion Shambler's exact 6-unit combat speed and choose the nearest eligible same-team corpse for its melee and Consume Corpse phases; packaged class and ability data prove locomotion and both ability contracts but not the retired noun phase's corpse-selection policy.
- campaign-carrion-corpse-fade: Publish ObjectDelete at Consume Corpse's exact 2.133333-second SetCorpseFadingAway point; ability bytecode proves the fade call and timing, but the original server sender for the native corpse-fade flag is unavailable.
- campaign-pouncing-stalker-selection: Use Pouncing Stalker's exact Jump Attack whenever its selected target is outside Swipe range, then use Swipe once adjacent; packaged content proves both actions and Jump's locomotion speed, but the retired phase's selection cadence is unavailable.
- campaign-pouncing-stalker-jump: Drive Pouncing Stalker's rendered root with authoritative position/orientation snaps throughout speed-35 travel and at landing, without competing client pursuit; the native ObjectJump packet's four physics operands and exact airborne arc remain unavailable.
- campaign-citadel-basic-shield-movement: Use Citadel Basic Shield's exact 4-unit combat speed for its exact melee strike and do not invent a passive shield; packaged class, phase, ability, and melee-template data prove locomotion and that the phase contains only the strike, while any role-name-implied shield contract remains unavailable.
- campaign-verdanth-basic-ranged: Use the shared ranged action after the recovered 0.83-second FirstAggro_Turret presentation; the phase proves no Lua primary, while the retired native/class-data homing-shot range, cadence, speed, damage, and collision policy are unavailable.
- campaign-verdanth-special-two-selection: Reevaluate the exact LowAllyHealth/Direct Heal branch whenever Verdanth Special Two reaches its repeatable action producer, otherwise use Entangle, with its exact 6-unit combat speed; packaged class, phase, condition, and ability data prove locomotion, both actions, and their operands, but the retired phase's polling cadence is unavailable.
- campaign-noct-basic-melee-dog-distraction: Retry Noct Basic Melee Dog's phase after 100 milliseconds when the exact one-percent `NoctDogIsDistracted` draw suppresses its bite; packaged condition bytecode proves the random threshold and suppression branch, but the retired phase engine's polling interval is unavailable.
- campaign-verdanth-basic-healer-movement: Use Verdanth Basic Healer's exact 5.5-unit combat speed and reevaluate the authored ally condition at each repeatable action boundary; packaged class, phase, and ability data prove locomotion and both action contracts, but the retired ally-condition polling cadence is unavailable.
- campaign-nomad-special-one-charge: Use Ragetusk's exact 4-unit combat speed plus the authored three-unit charge boost, select the authored landing outro and immediate follow-up on successful arrival, end canceled server pursuit without a synthetic halt packet, and project the exact two-unit knockback as navigation-safe horizontal relocation with `react_knockup`; packaged class and ability data prove locomotion, the charge, failed-movement halt/wait branch, follow-up strike, and knockback operands, but the pursuit scheduler has no failed-result boundary and the current forced-movement adapter has no airborne 4-5-unit arc projection.
- campaign-first-visit-boss-nouns: Select the position-aligned base boss cataloged in `notes/campaign/mobs.md` and the exact captain entry authored in each later difficulty band rather than synthesizing suffixes; content proves every selected noun is an exact entry in its intended level and band, but the public named-captain association is not decoded from a localized noun field.
- campaign-overdrive-energy: Refill unlocked Overdrive to 100 once per mission and permit one activation; build 103 proves the 100-point maximum and 3.3-point active drain, but no recovered initial-snapshot or post-use buildup policy determines later mission energy.
- campaign-first-visit-boss-profile: Use the package-exact complete Weaver/`Rezzer_Captain`, MGP-2/`CitadelSpecialThree_Captain`, and TCD-24/`CitadelSpecialTwo_Captain` profiles; for other mapped named captains, combine the exact same-family combat profile with captain-authored metadata and retain rank-shaped scale and footprint only because their noun-physics assets remain unavailable.
- ghost-form-movement: Keep Ghost Form movement speed neutral while applying its recovered six-second dodge/incorporeal protection; the shipped `modifier_ghostform_movebuff` dependency and its movement operands are absent from every build-103 package.
- raknet-retransmission: Retain at most 4096 reliable datagrams per peer, retry after 500 ms with exponential backoff capped at 2 seconds, and retire the peer after eight failed sends; build-103 adaptive retry, congestion feedback, and exact window limits have not been recovered.
- campaign-interactable-range: Use a conservative 12-unit script-interactable admission radius; live playtests show the former 8-unit fallback makes obelisks appear unresponsive, and the exact retail server radius remains unavailable.
- campaign-1-2-population: Preserve one observed Quantum or Bio roster theme per first-visit route floor but reuse the bounded 15-actor lieutenant-cluster planner; build-103 content proves the six noun identities and authored loci, while the retired server's floor theme roll, density, and cluster allocation are unavailable.
- campaign-1-3-population: Preserve one observed Necro or Plasma roster theme per first-visit route floor but reuse the bounded 15-actor lieutenant-cluster planner; build-103 content proves the six noun identities and authored loci, while the retired server's floor theme roll, density, and cluster allocation are unavailable.
- campaign-2-3-population: Preserve one observed Quantum or Cyber roster theme per first-visit route floor but reuse the bounded 15-actor lieutenant-cluster planner; build-103 content proves the six noun identities and authored loci, while the retired server's floor theme roll, density, and cluster allocation are unavailable.
- campaign-zelem-chargeup-cycle: Use Zelem Basic Chargeup's exact 7-unit combat speed, make Build Charge initially eligible and repeat it with symmetric 16±0.5-second variance, consume its unique stored charge at the next valid melee opportunity, and use Standard Attack between charge windows; packaged class, ability, and modifier data prove locomotion, all three actions, and the stored-charge transition, but initial eligibility, variance distribution, and polling order are unavailable.
- campaign-2-4-population: Preserve one observed Quantum or Necro roster theme per first-visit route floor but reuse the bounded 15-actor lieutenant-cluster planner; build-103 content proves the six noun identities and authored loci, while the retired server's floor theme roll, density, and cluster allocation are unavailable.
- sage-companion-pursuit: Acquire hostile NPCs within 10 units and pursue at 6 units per second before using the authored Support Healer Pet Basic range; a live report proved the previous 20-unit acquisition radius pulled Dendrones too far from Sage, while packaged content proves `PetTargeting`, follow-owner behavior, and the attack graph but does not expose the retired server's exact acquisition radius or movement speed.
- stationary-death-delay: Retire destroyed stationary fixtures after a one-second fade instead of the ordinary NPC 15-second corpse lifetime; exact build-103 fixture teardown timing is not recovered.
- campaign-nashira-fiend-cadence: Allow one Blasting Fiend portal every 12 seconds while retaining the authored 12-minion ceiling; packaged Shadow Toss and Shadow Boss Passive bytecode prove the per-impact callback and ceiling but expose no pacing rule that reconciles the observed excessive every-lob spawning.
- campaign-nashira-illusion-presentation: Keep the attached duplicate shader on illusions until removal and reserve fiend summoning for the original, making copies distinguishable without removing their attacks; packaged Lua only proves a temporary split shader and does not establish this persistent visual/no-summon policy.
- npc-projectile-height: Launch and aim NPC projectiles at the greater of footprint height or authored vertical-bound center instead of ground origins; exact build-103 muzzle hardpoint selection is not recovered.
- campaign-toss-center: Apply authored toss muzzle offsets above the ground origin using the greater of hero footprint height and indexed vertical-bound center; native GetCenterPoint proves half scaled noun height, but the exact noun-height operand is not yet projected into the runtime physics catalog.
- npc-action-fallback: Until each content-authored NPC ability is decoded, unknown non-fixture campaign nouns use a shared ranged or melee action selected from noun-role tokens so later levels remain playable; exact ability, animation, timing, and damage selection remain unavailable.
- campaign-homing-collision: After the authored one-second unguided delay, retain and hit the original live target at the initial direct-flight deadline only while it remains within the 18-unit travel budget, using the recovered homing flag one and zero turn-rate/turn-acceleration operands; Lua and client natives prove those inputs and target following, but the retired server's exact curved collision sweep and arrival timing remain unavailable.
- campaign-zunh-adds: Spawn two deterministic agent escorts at the nearest authored horde loci for first-visit Zunh, falling back to three-unit boss-anchor offsets only when two loci are unavailable; walkthrough footage proves plural adds despite the zero-add pB listener graph, but not their nouns, exact count, positions, budget, phase timing, or publisher.
- 3-3-verdanth-skeet-phase: evaluate Caustic Stinger's package-exact 0.5 Darting Attack branch before conditionless Circle Target using the shared scheduler cadence; use its exact 7-unit combat speed, authored +6 charge increase, template-exact `2 * source footprint` movement stop, and strict two-unit final hit gap, while packaged Circle Target owns its recovered random 1-2-second, radius-7-9, 0.2-second navigation loop. Only the retail outer phase poll cadence remains unrecovered.
- 3-3-verdanth-picky-selection: use the current authoritative target with the exact 10-unit condition boundary and exact 4.5-unit combat speed for the Charging Brute phase; packaged class, AIDef, condition, and ability records prove farthest-hostile selection within 30, the separate melee fallback, ranked cooldowns, +3 charge speed, 4.5-unit landing transition, final 0.75-unit approach, and both damage contracts, but the shared outer phase cadence remains unrecovered
- 3-3-verdanth-basic-melee-slow: preserve Chrono Striker's exact opaque native slow modifier GUID with a conservative two-second server lifecycle; build 103 contains no matching modifier Lua, package resource, or executable definition from which its duration and movement penalty can be recovered
- campaign-exploder-scarab-detonation: Trigger the native-only Scarab blast at footprint contact, use a four-unit sphere and 5-8 Technology/Energy damage with coefficient 0.05, publish the existing cyber bomb explosion, apply the documented four-second base daze as movement slow and three-second later-rank stun, then route its self-death through ordinary encounter and loot cleanup without awarding a player kill; packaged build-103 content proves the three difficulty nouns, activation timings, 4/2.5 combat/noncombat locomotion, low-health role, base daze versus later-rank stun, and that every nearby hero is damaged, but the retired `CitadelMinionSuicide` native supplies no recoverable blast radius, damage range, proximity threshold, low-health threshold, or explosion event.
- campaign-exploder-scarab-rarity: Retain 25 percent of uniformly selected Exploder Scarabs outside 4-1 and reroll the remainder from ordinary minions; live balance feedback requires the high-damage unit to be rare, but the retired director's exact selection weight is unavailable.
- campaign-laser-tank-obstruction: Treat a navigation route longer than the direct beam by 20 percent plus two actor footprints as an obstruction and force repositioning; the retired server's exact beam-versus-world collision query is unavailable.
- campaign-undermind-native: Use a 12-unit fiery strike with 2-4 Elements/Energy damage, six-second 2-4 burning ticks, three 10-percent Energy-vulnerability stacks, allied fiery-buff presentation, and ranked 4/3/2-second cadence; packaged content proves the three intended effects, 8.5/8.5 locomotion, first-aggro presentation, and the 1.866667-second Burning DOT cast with contact at 0.6 seconds, but the retired `nBehavior_FleeFacing` implementation hides attack selection, numeric damage, status, ally-buff, and cadence operands.
- campaign-suppression-mechanoid-stun: Project both opaque rank modifiers carried by Homing Stun as the shared three-second `StalkerShock` lifecycle; packaged Lua proves the projectile, damage, ranked cooldown, homing contract, and two modifier GUIDs but the indexed content does not expose their stun operands.
- campaign-suppression-mechanoid-aura: Refresh `SilenceModifier` every half-second for heroes and companions within ten units; the long description proves a proximity silence aura but does not expose its native radius or refresh cadence.
- campaign-citadel-homing-delay: Begin Citadel Specific Four and Suppression Mechanoid homing after one millisecond; their long descriptions prove target tracking but do not expose a delayed-acquisition operand.
- campaign-scaldron-dog-strafe: Approach 1.5 units to a deterministic lateral side of the target before biting; the long description proves strafing while closing but does not expose its native lateral distance or side-selection policy.
- campaign-reparatron-repair-adapter: Select the first defeated non-fixture inside the exact radius-20 scan, delay the repair consequence through the combined alert/repair duration, and classify Basic/Specific/native-minion noun families for the exact 2/1/1 versus 6/4/2 stack thresholds; packaged Lua proves the scan order, Technology-only eligibility, pursuit/revalidation sequence, and thresholds, but current zone snapshots do not expose NPC damage genesis as a repair eligibility field and the repair adapter does not yet project corpse-follow movement or ten-second death-timer refreshes.
- co-op-scaling: freeze NPC health and damage scaling to the admitted participant count for the zone lifetime; retail disconnect-time rescaling policy is unavailable
- co-op-playgroups: use the recovered build-103 Playgroups tags and conventional empty optional fields for the first native party envelope; no retail two-client Blaze capture proves every optional structure or notification ordering yet
- co-op-raknet-roster-order: answer each local Hello, exchange connected slots with PlayerJoined, and send PartyMergeComplete only when the connected slot mask equals the frozen roster; client handlers and the incomplete-roster crash constrain this safe order, but no retail multiplayer capture proves the exact message grouping or timing
- campaign-animus-flee: Animus uses the existing three-attempt navigation-clipped 3-6 unit flee sampler before Ghostly Bolt when a hero is within 10 units; the packaged AI names FleeFacing but the retail server destination policy is absent
- campaign-ranged-strafe: ranged enemies whose descriptions require strafing use three navigation-clipped 3-6 unit lateral attempts between projectile cycles; packaged AI names strafe behavior but the retail server destination scorer is absent
- campaign-pouncing-self-resurrect: Pouncing Stalker's indefinite IsSelfResurrect sign consumes its first lethal hit and restores 40 percent health; the retail fluctuating window and restored-health fraction are absent
- campaign-dynosphere-shield: Dynosphere uses rank-shaped 10/20/30 absorption and the established 12/10/8-second recharge cadence; its class description and shield effects are exact but the native shield amount and timer are absent
- campaign-ragetusk-enrage: Ragetusk gains up to five nearby-ally-death stacks, each adding 15 percent damage and eight percent body size inside 12 units; build 103 proves the listener, growth, and increased damage but withholds its native radius, magnitude, and cap
- campaign-necrotic-leech-heal: Necrotic Leech heals for 25 percent of committed damage dealt to a living ally inside 12 units; build 103 proves the allied-damage listener but withholds its radius and heal coefficient
- campaign-shade-drifter-aura: Shade Drifter pulses an eight-unit 1/2/3-damage supernatural-energy life-drain once per ranked charge cycle; build 103 proves the nearby drain aura but withholds its radius, tick cadence, ranked damage, and healing coefficient
- campaign-sting-raider-shift: Sting Raider's first area hit activates a four-second shifted state that reduces subsequent area damage by 75 percent; build 103 proves strong area resistance in shifted form but withholds the native trigger duration and reduction
- campaign-laser-tank-energy-defense: Laser Tank uses rank-shaped 250/500/750 Energy Defense shared with Fragbot Mech, converts that rating through defense / (defense + 100), and reduces damage-over-time ticks by 75 percent; its long description proves high Energy and damage-over-time resistance but does not expose the native reduction curve or damage-over-time operand
- campaign-strafing-drakon-haste: Strafing Drakon gains 25 percent movement speed and a 25-percent shorter attack cadence while another living same-species ally is inside ten units; build 103 proves the proximity haste but withholds its radius and magnitude
- campaign-ray-killer-flee: Ray Killer uses the established three-attempt navigation-clipped 3-6 unit flee sampler after an accepted hit before firing again; build 103 proves hit-triggered fleeing but withholds the native destination policy
- campaign-trioculist-chain: Trioculist arcs through at most two additional targets inside five units and halves damage on each jump; build 103 proves diminishing nearby lightning arcs but withholds the native jump radius, target count, and attenuation
- tutorial-horde-budget: Use four mixed authored tutorial enemies at distinct arena positions in each of the four content-proven waves; the previous two- and three-enemy fallbacks were live-reported as too sparse, while the retired server's exact caller budget, noun costs, selection, and per-wave counts remain unrecovered.
- tutorial-quadra-recovery: After every third completed four-shot Burst Shot volley, hold Tutorial Special One in `wander_flavor` for 4.4 seconds, reset animation, then resume its authoritative action loop; packaged Burst Shot and generic combat-idle content prove no exhaustion phase, while live feedback requires Quadra's periodic sleep presentation and the retired server cadence is unavailable.
- pvp-local-unlock: Grant the first PVP deck unlock and copy a complete active PVE trio into the first unused unlocked squad as its initial PVP deck so build 103 enables and can accept its native PVP destination; the client predicate and zero-cost unlock ID 38 are recovered, but the retired level-10 purchase request and starter-PVP-squad policy are not.
- pvp-basic-damage-roll: use the midpoint of the client-authored projected basic-damage range for the initial Arena authority until the retired server's per-match PvP random-stream seeding is recovered; enemy-team, range, resource, and damage-reduction checks remain authoritative.
- pvp-active-damage-shape: apply authored special-two and random ability damage to the selected opposing hero as a single-target Arena fallback while preserving mana, cooldown, release, range, and damage-reduction authority; packaged ability content proves each campaign shape, but no retired-server capture proves its PvP-specific area, projectile, teleport, or status policy.
- pvp-melee-range-tolerance: admit Arena melee basics within the authored surface range plus the client's recovered three-unit held-melee cursor radius because independently reported player poses can remain several units apart when the native client considers pursuit complete; replace this tolerance when a retired-server Arena capture proves its latency-compensation policy.
- pvp-developer-result-header: for the `/victory` Arena result probe, select the invoking player's team, report two round wins in that team's corresponding header count, and mark its first two per-player rounds with the client-proven winning outcome `3`; the exact retired-server selector/count policy and losing outcome remain unrecovered.
- social-search-presence: project active authenticated account searches as Navigation presence level 4; the HTTP account search does not yet consume the exact live Blaze presence state.
- campaign-projectile-target-bounds: When a campaign NPC has no indexed noun bounds, derive a centered horizontal target box and twice-footprint height from its recovered positive footprint; detailed build-103 bounds for the full campaign noun set are not imported.
- campaign-cashout-rare-chance: Use the client-exact per-mission medal averages of one per Bronze, three per Silver, and six per Gold as the displayed and rolled Rare chance, rounding fractional averages down; the retired server's additional chain-length baseline and full rarity formula remain unrecovered.
- campaign-cashout-daily-bonus: For the first durable Cash Out after the client-exact 22-hour cooldown, multiply the unrounded medal-derived Rarified-or-better chance by the patch-note-proven 1.5 and derive Purified chance from that boosted band; build 103 proves the last-claim timestamp and presentation flag, but the retired server's rounding order, Purified treatment, and completion-versus-cash-out claim boundary remain unrecovered.
- campaign-cashout-purified-chance: Set Purified chance to one fifth of the medal-derived Rare chance capped at ten percent and avalanche each result/reward identity into a separate replay-stable roll; the wiki proves a zero-to-ten-percent red Cash Out band but the retired server's exact Purified formula and random-stream seed remain unrecovered.
- campaign-catalyst-chain-state: Carry each member's catalyst grid across Continue, initialize first-use capacity to three through the purchased nine-slot ceiling, enable diagonal rows only after the account upgrade, and clear the grid with terminal chain teardown; the client and wiki prove chain lifetime and unlock capacity but the retired server's disconnect-abandon grace policy is unavailable.
- campaign-coop-loot: Multiply catalyst attempt challenge by active participant count and apply a collected health or power capsule to the collector's complete squad plus every allied active hero; the wiki proves increased co-op catalyst quantity and allied-squad capsule effects but the retired server's exact player-count scalar and reserve-ally replication contract remain unavailable.
- campaign-active-roster-loot: Select ordinary and Cash Out item class/science eligibility across every activated account hero instead of only the currently deployed hero; the wiki proves the eligibility boundary, but the retired server's weighting within that roster remains unavailable.
- pterodyne-stealth-recovery: Start Stealth Attack's authored 10-second cooldown on reveal, including travel expiry, to guarantee a visible recovery window in the ordinary attack cycle; chunk 1028 proves the duration but not the retired phase selector's cooldown anchor.
- campaign-npc-pursuit-projection: Allow NPC pursuit and NPC-owned charge, strafe, circle, flee, resurrection, knockback, clone, copter, and ooze endpoints to project up to 12 units onto the nearest compatible navigation layer while retaining strict hero movement validation; repeated live reports prove authored enemies can begin or finish abilities outside the ordinary three-unit envelope, but the retired server projection distance is unavailable.
- campaign-carrion-consume-cadence: After a successful Carrion Shambler meal, suppress another corpse selection for 15 seconds so it returns to combat even when multiple corpses remain; the packaged six-second consume action cooldown is known, but the retired AI selector's combat-versus-corpse weighting is unavailable.
- campaign-raytheoid-piercing-endpoint: Resolve Raytheoid's Piercing Laser at its selected target while retaining contacts between the source and target; live play proves the previous 50-unit terminal delay was visibly wrong, but the retired server's post-target piercing distance is unavailable.
- campaign-lieutenant-combat-walk: Route ranged captain and lieutenant projectile cycles through a 50-percent navigation-bounded two-to-three-second lateral strafe, give Magnetic Master a straight retreat followed by a distinct lateral strafe, and exclude melee captains; repeated 0.6.10-0.6.12 captures prove the missing/short combat walk and Magnetic Master's separate post-flee movement, but exact per-family walk chances, durations, and headings are not recovered.
- nashira-shadow-toss-height: Interpret Nashira's recovered local Shadow Toss offsets with a three-unit world-height release for both near and far casts while retaining the exact horizontal operands and 12-unit arc; live 0.7.3 reports prove the previous negative far height placed the skull below its visible cast and hit path, but the retired server's local-axis conversion is unavailable.
- party-relay-single-endpoint: use one server endpoint and relay only recovered PvP capability/ready packets for the first two-member path; no client evidence yet proves how three or four identical roster endpoints preserve distinct virtual peer identity
- slowing-melee-snare: Apply a 50-percent movement penalty for the existing two-second Slowing Melee modifier lifecycle; shipped content exposes only opaque modifier GUID `0x83CD6CBC`, so the retired server's exact movement operand remains unavailable.
- campaign-mending-tanglid-flee: Repeatedly retreat Mending Tanglid three to six navigation-clipped units while a hero is inside ten units, after first checking for a wounded ally; live play proves persistent avoidance rather than Magnetic Master's one-shot retreat, but the retired server's exact keep-away radius and destination scorer are unavailable.
- critical-npc-animation: Pair the recovered fast critical corpse lifecycle with each noun's indexed rig-compatible death state; raw CharacterAnimation resources retain later descriptor entries, but their exact descriptor-to-state projection is not yet indexed.
- hero-swap-knockback: On replacement beam-in, push eligible nearby Game four units at speed 20 inside a six-unit radius while excluding fixtures, bosses/Destructors, and `Surefooted_NPCAffixModifier`; no recovered packaged swap script exposes the retail radius, distance, or speed.
- captain-wave-delay: Publish a named Captain two seconds after its registered opening horde actors are terminal; packaged markers and reflected director state prove separate horde and Captain phases, but the missing retail server does not expose their exact clear-to-Captain delay.
- destructible-explosion-size: Select the packaged minor-instrument explosion below a 2.5-unit footprint/bounds estimate and the packaged gas-collector destruction effect at or above it, retaining wrecks for 1.5 or 2.5 seconds respectively; packaged Lua proves each endpoint effect but no recovered cross-family selector or intermediate size table.
- campaign-merak-add-combat: Give damage-triggered `CryosPlasmaAdd.Noun` summons their recovered persistent `fire_wall_minion_onfire_effect` trail plus the closest ranked Cryos fiery-melee action after recovering Merak's exact summon threshold, distance, noun, passive, and population cap; the add's own retired attack selector remains unavailable.
- campaign-destructor-footprint: Give every Destructor at least the packaged Zelem and Verdanth 2.7-unit combat footprint in campaign and developer-spawn paths; live 0.7.20 melee reports cover Orcus and The Corruptor, but exact physics for the other four Destructor nouns remain unavailable.
- campaign-corruptor-portal-anchors: Prefer imported `BossPortal` or `EnemyPortal` markers within 50 units of the Corruptor and otherwise place three `EnemyPortal.Noun` actors on an eight-unit ring; packaged Lua proves cloning every runtime `kType_BossPortal` position, but that object-type classification is not retained by the prepared campaign director.
- hint-compass: map positive Y to north and positive X to east; client and content evidence do not label world axes with cardinal directions
- nocturna-thorn-exit: Delete the authored thorn marker objects once their complete owning marker set is clear; indexed content exposes no thorn-specific exit animation or effect.
- voltroid-charge-friend-cooldown: Pace Charge Friend at 3/2.5/2 seconds by rank; the packaged ability cooldown is zero and the retired server AI decision cadence is unavailable.
- malformed-selected-pve-squad: Preserve distinct owned members and fill invalid selected-group positions from stable owned-creature order during login; the report proves the native client rejects the projected group locally but its trace does not expose the malformed persisted IDs or the retired server's repair selection policy.

- npc-facing-placement: Preserve exact spawn-marker rotations and use valid zero-Euler facing for additional generated placements until an eligible introduction or action turns toward its target; ordinary retail procedural heading selection is not recovered.
- arcturus-phase: prefer Saw Blade Shot outside laser range, start the first launch after 30 seconds, land at the original encounter center, and hide at two seconds; native selector arbitration and center-placement tolerance are not recovered.
- arcturus-turret-orbit: use server-owned horizontal beam markers with a 24-second rotation and shared navigation-clipped hit segment; native OrbitingBeamMarker angular speed and orbit geometry are unrecovered.
