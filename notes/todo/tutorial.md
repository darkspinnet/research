# Tutorial

- Current: Shared zone authority owns gameplay and Beam Out; the authored Ride the Lightning unlock presents its one-use “Press 1” lesson; Blitz criticals and Deadly Precision activate together at the authored passive lesson; Sage's saplings start autonomously at spawn; tutorial enemy drops suppress DNA, catalysts, and gear while the authored health-and-power marker creates its ten fixed capsules and enables later enemy capsule drops; accepted obelisk use stops Blitz, plays his interaction animation, and retains the open/drop timeline through release; held-click basics retain one pursuit/action owner without redundant redirects, stale recovery, or right-click locomotion; NPC first-aggro presentation is one-shot; each final-arena wave admits three enemies; accepted Beam Out persists progression and publishes one positive tutorial completion snapshot that atomically returns the client to the ship.
- 2026-08-19: Report `18:06:04` reached ship scene-ready and UI-ready before access violation `0xc0000005`. The exit response had stacked positive `TutorialGameMsgs` and `QuickGame` state transitions; active generic gameplay treats both as state changes. The temporary QuickGame-only correction was superseded by the 2026-08-29 evidence below.
- 2026-08-18: A live `/victory` run reached `PlayerStatusUpdate(status=32, progress=1)`, then the delayed positive `TutorialGameMsgs` snapshot reproduced the permanent black screen and prevented the proven Blaze remove-player reason-6 return. At that checkpoint, accepted Beam Out was reduced to the native hero beam presentation while completion remained at the authenticated Blaze boundary; the later QuickGame return evidence supersedes that temporary policy.
- 2026-08-18: Restored the final arena's authored four-wave horde lifecycle: arena entry emits the incoming alert and starts a two-second timer, each wave clear starts the next wave after 1.5 seconds, and the fourth clear emits the defeated alert before completing the tutorial two seconds later. The 2026-08-19 `18:04:00` report superseded the initial two-enemy fallback with three enemies per wave.
- 2026-08-18: Steam 0.5.2 logs showed tutorial setup aborting at status 8 because `TutorialSloth.Noun` has no fixed `_ai` placement. Campaign directors now retain the full imported NPC profile catalog, and horde planning resolves all wave nouns from it instead of treating fixed-world actors as a complete profile catalog.
- Remaining: Tutorial-only persistence, no-reward presentation, and human validation.

# 2026-08-29 tutorial completion return correction

- A `0.7.2` report captured successful Beam Out persistence followed by delayed `AF 00`, a scene-ready/UI-ready ship, an inventory refresh, and access violation `0xc0000005`.
- A second `0.7.2` report reproduced that exact `AF 00` crash, then showed two later launches rejoining the completed tutorial's retained game `1`, receiving the initial chain vote, timing out during account-profile loading, and crashing again. The delayed native completion step now retires the completed tutorial game shell before publishing its atomic snapshot-and-ship transition, releasing the Crogenitor from stale gameplay membership.
- Native `sub_4507C0` proves positive `TutorialGameMsgs` subtype `0` already updates cumulative XP, derives the account level, writes onboarding progress `3000`, and selects ship state `2` in the same handler.
- The delayed terminal publication now sends only that positive completion snapshot. It no longer substitutes `AF 00` (stale in-memory progress) or stacks both transitions (double-transition crash).
- Future launcher reports record the selected Crogenitor and whether cinematic skipping was enabled; the attached report's Fang state proves skipping was enabled, while creature, UI, and HELIX sound continued, so it does not establish a global audio failure.
