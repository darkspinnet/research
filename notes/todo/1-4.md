# Campaign 1-4

- 2026-09-06 death correction: send the rig state `shadowboss_dead`, not raw clip `nct_boss_su_shadowboss_death`, while preserving Nashira's eight-second presentation window; see `notes/combat/nashira-death.md` for package evidence and verification limits.

- Current: Chain 1-4 selects Nashira's exact `ShadowBoss` noun as a standalone zone boss with the authored first-aggro cinematic, ranged Shadow Toss and Blasting Fiends, varied close Shadow Boss Swipes, terrifying roar, one-time two-clone split, and rig-correct death, while the later `nocturna_1` occurrences retain their ranked captain identities, roster-authored affixes, and elite health, damage, and scale promotion; human validation remains.
- 2026-08-29 Destructor audit: disassembled `FirstAggro_ShadowBoss` (`Abilities/0x9C1C4760.lua`, SHA-256 `aac8f9f0a76452f49000107fa1ec5ce54e899b0e33d5aeaa1f597f0fb2c060cd`) and restored its `black_hole_effect` notification at the authored two-second boundary instead of dropping or prematurely publishing that scripted event.
- 2026-08-29 intro correction: the 0.7.1 checkpoint-restore report reached Nashira's correct boss marker but exposed her before `FirstAggro_ShadowBoss`'s two-second start boundary, allowing ordinary movement to replace the entrance; Nashira now remains hidden until that boundary, then reveals with `black_hole_effect` and `shadowboss_onaggro` while combat remains held for the complete 9.6-second sequence.
- 2026-08-29 0.7.3 correction: delayed first-aggro actors now enter with no combat target and remain untargetable until the reveal packet restores their target, preventing Nashira's client AI from walking while hidden. Shadow Toss now launches three units above her origin for both near and far casts so the skull trajectory agrees with the visible release and landing path, and Blasting Fiends fall back to the active hero when the boss target transiently clears at impact instead of aborting the lob publication group.
- 2026-08-29 0.7.4 correction: report object 235 remained at server position `(-235.359,432.047)` while the client repeatedly targeted Nashira at `(-210.284,424.641)`, a 26.164-unit disagreement caused by the two-second reveal restoring her combat target during the remaining 7.6 seconds of the held intro. Delayed reveals now keep the actor noncombat and untargetable through the complete first-aggro boundary; activation then resets the entrance animation, republishes the authoritative transform and locomotion goal, and restores the combat target before pursuit or attack.
- 2026-08-29: Recovered Nashira's packaged first-aggro, Shadow Toss, and Shadow Boss Swipe contracts from `ServerData.package`; the boss now holds combat for the 9.6-second radius-100 intro, lobs the exact shadow projectile on its 12-unit arc at range, and changes between ranked ranged cadence and the authored close swipe instead of using the generic drone punch fallback.
- Nashira evidence: Both unique 0.7.0 reports identify object 486 as `ShadowBoss.Noun`, while traces show only the generic range-2.5 pursuit and no boss attack. Packaged `FirstAggro_ShadowBoss` supplies `shadowboss_onaggro`, 6.6-second animation, two-second start delay, one-second end delay, and radius 100; `ShadowToss` supplies ranked 6/5/4-second cooldowns, 16/18/20 range, 18-24/24-30/30-36 Supernatural Energy damage, 0.93-second launch, two-second release, 12-unit arc, exact projectile, and shadow effects; Shadow Boss Swipe supplies ranked 5/4/3-second cooldowns, six-unit reach, 12-24 Supernatural Physical damage, and its large hit effect.
- 2026-08-29: Replaced the 1-4 Khoo captain surrogate with `ShadowBoss.Noun`, classified Nashira as a standalone boss without captain elite scaling, preserved the ranked Mizod and Kane captains at 10-1 and 18-2 with their Spiky/Spiky Aura and Reflective/Reflective Aura identities, and restored authored-anchor proximity admission for standalone zone-boss encounters after the general proximity path was gated.
- 2026-08-26: A successful Carrion Shambler meal now starts a separate 15-second corpse-selection lockout, allowing its ordinary combat action loop to run while additional corpses remain. Nashira's reported 1-4 position is within the generic boss-anchor admission radius and is covered by the restored later-boss publisher.
- Evidence: Pouncing Stalker exposes and consumes a one-use self-resurrection sign, reserves one fresh action generation, publishes its navigation-resolved landing transform after the speed-35 pounce, and resumes combat; Pterodyne and Khoo use decoded 6/4 movement in both melee and Stealth Attack, with Pterodyne carrying its reserved generation into the next action and publishing supernatural stealth before the chained Fear Nova scream. Every Carrion Shambler stack publishes the authored +0.06 BodyScale and resets it with modifier expiry or teardown. Muting Leucopod now replaces the unsafe continuous relocation loop with one transform-synchronized bounded strafe or short idle per projectile cycle. Close-range pursuits accept client navigation-height projection differences, and every attack boundary publishes a stationary movement goal.
- Remaining: Human-validate natural traversal, bounded strafing, and completion, then replace the documented route, phase-cadence, Khoo publisher, objective-selection, and corpse-fade compatibility fallbacks if authoritative retail-server evidence becomes available.

## 2026-08-29 Nashira behavior and death crash

- Crash evidence: the 0.7.0 report records Nashira object 486 dying before the client later exits with access violation `0xc0000005`; the prior death adapter had no Shadow Boss physics entry and therefore selected the unrelated `zlm_minn_sp_3_death_melee` fallback.
- Client evidence: indexed `ShadowBossPassive` data carries health gates `0.66` and `0.33` with cumulative duplicate counts `1` and `2`, plus the owned-minion cap `12`; its damage callback advances duplicate readiness through those gates and `ShadowBossDuplicateCheck` consumes one permitted duplicate action at a time. `ShadowBossDuplicate` names the split animations and duplicate effects, while the animation catalog identifies `nct_boss_su_shadowboss_death` as Nashira's rig-specific death.
- Implementation: the original Nashira runs one split at 66-percent remaining health and a second at 33 percent, creating one owner-bound, lootless illusion after each 1.7-second split boundary. Illusions clear boss/elite identity and introduction fields, cannot recursively split or summon fiends, and disappear through owner-death cleanup. They copy remaining health and receive fourfold physical/energy damage: indexed `ShadowBossPassive` (chunk 597) implements `SetIsDuplicate` with `duplicateDamageIncrease = 3` for both incoming damage attributes, replacing the earlier one-third-health fallback. Spawn attaches the duplicate shader through the passive-effect slot, including re-publication, instead of sending an unbound one-shot event. Keeping that shader on illusions rather than briefly applying it to every form is a documented presentation fallback. Split animations remain `shadowboss_split_a` and `shadowboss_split_b`; authored shuffled strafe destinations at 6-8 units and speed 20 remain outside this fix. Only the original entrance retains the radius-100 cinematic.
- Implementation: Shadow Toss uses the complete authored `(3, 5, -2)` far and `(-5, 3, 2)` close muzzle offsets, while rank-matched Blasting Fiend portals are paced by a deterministic 12-second gate and retain the authored 12-active-minion ceiling. Close combat rotates through all four authored Shadow Boss Swipe states, and `cast_shadow_panic` applies the existing Terrified authority while its authored animation drives the roar, fear effect, and camera vibration.
- Implementation: Nashira and her duplicates are treated as animated boss creatures for death presentation even when admitted through Destructor provenance, preventing critical hits from selecting the fast corpse path; final Nashira defeat now holds corpse fade and the boss-complete director transition through the animation catalog's 227-frame `nct_boss_su_shadowboss_death` boundary (rounded up to eight seconds).
# 2026-08-26 continued-session and boss regressions

- The 0.6.5 reports entered 1-4 through Continue with the prior result session still attached and Wraith's depleted 17.733 power incorrectly promoted to the new maximum; Continue now clears the result state and restores every hero to its established maximum power.
- The player reached within 2.917 units of the authored `nocturna_1` boss anchor without an admitted boss. Later-level boss proximity admission now publishes the packaged `pB` listener graph there.
- The apparent immortal-enemy report was repeated Wraith basic-attack rejection under Silence; left-click attacks now remain usable while secondary and support powers stay silenced.
- Pouncing Stalker's one-use self-resurrection aura was attached in slot 16 but never removed when consumed, leaving its rings after the final death; the resurrection transition now removes that attachment before restoring health.
- Pouncing Stalker's lethal-hit recovery now publishes the packaged `gen_death_resurrected` state and defers its next combat action for the animation's authored 62 frames (about 2.07 seconds), preserving the embedded resurrection voice and flash events instead of immediately overriding them with another attack.
- Pouncing Stalker's post-pounce Swipe now carries its reserved action generation through the cooldown, allowing the next selector pass to pursue a moved target and prepare another Jump Attack instead of discarding the follow-up as stale.
