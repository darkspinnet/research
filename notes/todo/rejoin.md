# Rejoin

- Current: Reconnect publishes a revision-stable live baseline; same-ID Blaze admission replaces stale game and party session projections without changing stable slots; restart restoration recreates durable game membership, world state, exact active-hero health, mana, pose, stealth, earned mission XP, committed security route, and teleporter presentation, plus the ordinary Blaze topology/capacity envelope, including the tutorial's valid difficulty-zero checkpoint. Authenticated account discovery activates checkpoint-backed membership before publishing `current_game_id`. Durable Continue now uses `GamePrepareForStart`, the normal client-ready statuses, and `GameStart` before publishing restored state because a restarted client has no live dungeon scene; only a retained live-client transport rejoin uses `ReconnectPlayer(GameDungeon)` after its revision-stable baseline. A stable completion identity and durable account receipt make repeated post-restart completion XP idempotent. Start Fresh remains the proven durable checkpoint tombstone.
- Remaining: Human verification of immediate native launcher Continue, an authoritative Abort signal distinct from the reason-6 removal also sent during ordinary client close, broader co-op abandon ownership, and result checkpoint expansion for future mid-result boundaries.
- Checkpoint contract: [zone safe checkpoints](../zone/checkpoint.md).
